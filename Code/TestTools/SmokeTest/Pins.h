// Support file for this folder: the light and IR sensor pin numbers live here.

#ifndef PINS_H
#define PINS_H

#include <Arduino.h>

const byte PIN_LIGHT = A0;   // photoresistor (voltage divider)
const byte PIN_IR    = A2;   // IR distance sensor (analog out)

#endif
